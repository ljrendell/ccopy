# Copying only user commands from console text
Tools for extracting executable code from copied text from the R console, 
by removing output (including warning and error messages) and input line 
prompts (``"> "`` and ``"+ "``).
